# === OAuth Development Configuration ===
# Used by: npm run dev:oauth (localhost:3020 - direct server)
#
# CRITICAL: Security
# 🔐 Generate a unique 32-byte base64 encryption key:
#    node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# NEVER commit this file with real values to git!
TOKEN_ENCRYPTION_KEY="your-32-byte-base64-encryption-key-here"

# === LLM Providers (OPTIONAL - gracefully degrades without keys) ===
# Claude
ANTHROPIC_API_KEY=sk-ant-xxxxx

# OpenAI GPT
OPENAI_API_KEY=sk-xxxxx

# Google Gemini
GOOGLE_API_KEY=xxxxx

# === OAuth Providers (configure at least one) ===
# Redirect URIs configured for direct server (localhost:3020)

# Google OAuth
GOOGLE_CLIENT_ID=xxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=xxxxx
GOOGLE_REDIRECT_URI=http://localhost:3020/auth/callback/google
GOOGLE_SCOPES=openid,email,profile

# GitHub OAuth
GITHUB_CLIENT_ID=xxxxx
GITHUB_CLIENT_SECRET=xxxxx
GITHUB_REDIRECT_URI=http://localhost:3020/auth/callback/github
GITHUB_SCOPES=read:user,user:email

# Microsoft OAuth
MICROSOFT_CLIENT_ID=xxxxx
MICROSOFT_CLIENT_SECRET=xxxxx
MICROSOFT_TENANT_ID=common
MICROSOFT_REDIRECT_URI=http://localhost:3020/auth/callback/microsoft
MICROSOFT_SCOPES=openid,email,profile

# === Server Configuration ===
HTTP_PORT=3020
MCP_MODE=streamable_http
NODE_ENV=development

# === Redis (for session persistence and horizontal scaling) ===
REDIS_URL=redis://localhost:6379
